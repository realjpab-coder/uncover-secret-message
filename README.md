The program imports BeautifulSoup.
It finds the data it needs from the document and discards everything it needs before that.
Arrays for the message, data, character to display, x, and y coordinates are created.
Data is sorted into the data_list array and shortens the original data.
From the data, it is further sorted into their respective lists.
Using all the lists, the message is displayed.
